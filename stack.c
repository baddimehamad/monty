#include "monty.h"
/**
 * f_stack - prints the top
 * @a_head: stack head
 * @the_counter: line_number
 * Return: null return
*/
void f_stack(stack_t **a_head, unsigned int the_counter)
{
	(void)a_head;
	(void)the_counter;
	bus.lifi = 0;
}
